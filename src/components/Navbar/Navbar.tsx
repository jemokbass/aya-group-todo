import { FC } from 'react';

const Navbar: FC = () => {
  return (
    <header className="navbar">
      <div className="container">
        <a href="/">ToDo List</a>
      </div>
    </header>
  );
};

export default Navbar;
