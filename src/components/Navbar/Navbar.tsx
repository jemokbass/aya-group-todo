import { FC } from 'react';
import './Navbar.scss';

const Navbar: FC = () => {
  return (
    <header className="navbar">
      <a href="/">ToDo List</a>
    </header>
  );
};

export default Navbar;
